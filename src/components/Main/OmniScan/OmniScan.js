import React from 'react';

const OmniScan = () => {
    return (
        <div>
            <h1 style={{color:'goldenRod', textAlign:'center', marginTop:'200px', fontSize:'25px'}}>Have to add Omni Scan Component</h1>
        </div>
    );
};

export default OmniScan;